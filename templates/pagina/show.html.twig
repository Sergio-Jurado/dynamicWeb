{% extends 'base.html.twig' %}

{% block title %}Pagina{% endblock %}

{% block body %}
    <h1>Pagina</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pagina.id }}</td>
            </tr>
            <tr>
                <th>Titulo</th>
                <td>{{ pagina.titulo }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ pagina.image }}</td>
            </tr>
            <tr>
                <th>Contenido</th>
                <td>{{ pagina.contenido }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pagina_index') }}">back to list</a>

    <a href="{{ path('app_pagina_edit', {'id': pagina.id}) }}">edit</a>

    {{ include('pagina/_delete_form.html.twig') }}
{% endblock %}
